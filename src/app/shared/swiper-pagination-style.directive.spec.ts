import { SwiperPaginationStyleDirective } from './swiper-pagination-style.directive';

describe('SwiperPaginationStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new SwiperPaginationStyleDirective();
    expect(directive).toBeTruthy();
  });
});
